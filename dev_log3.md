# 开发日志 - 2026-08-25

## 今日完成
- [✓] 实现 Function Calling 工具调用，AI 能自主决定调用天气查询工具
- [✓] 将天气查询功能封装成 AI 可调用的工具函数
- [✓] 完成多轮对话 + 工具调用的完整链路（用户提问 → AI 决策 → 执行函数 → AI 生成回复）
- [✓] 实现 AI 自动提取城市名并调用真实天气 API
- [✓] 提交代码到 GitHub

## 今天踩的坑
- （暂无遇到报错，一切顺利）

## 明天计划
- [ ] 给 AI 增加更多工具（比如：查询当前时间、计算器、搜索等）
- [ ] 学习 RAG 基础知识（让 AI 能回答自己知识库以外的问题）

## 今天的感受（一句话）
今天是最有成就感的一天！AI 不再只是“聊天机器人”，而是真正能“动手做事”的智能助手了。这就是 AI Agent 的核心。

## 今天的技术翻译（对产品经理说）

今天给 AI 装上了“手”和“脚”。

以前它只会聊天吹牛，遇到不知道的事情（比如天气），要么说自己不知道，要么开始瞎编。

现在不一样了：当用户问“北京天气怎么样”时，AI 会自动判断“这个问题需要调用天气工具”，然后提取出“北京”这个城市名，调用真正的天气 API 拿到真实数据（26°C、晴天），最后组织成自然语言回复给用户。

也就是说，AI 能**自主决策**——什么时候该聊天，什么时候该查数据，它自己说了算。以后查机票、查库存、发邮件、订会议室，都可以用这种方式交给 AI 自动完成。

## 今天的数据思考

今天我做的功能是：**Function Calling + 天气查询工具**

1. **我怎么知道这个功能好用？**
   - 输入“北京天气”时，AI 能正确调用工具并返回真实温度，而不是说“我不知道”或瞎编一个温度。
   - 工具调用的完整流程在 3 秒内完成，用户等待时间可接受。

2. **如果不好用，我怎么发现？**
   - 如果 AI 总是提取错城市名（比如把“北京市”提取成“北京市区”），导致查不到数据，说明 Prompt 里对参数描述不够清晰，需要优化。
   - 如果工具调用成功率低于 90%，说明 AI 的决策逻辑有问题。
   - 如果用户问“今天适合出门吗”这种需要综合判断的问题，AI 应该能同时调用天气 + 空气质量等多个工具。

3. **如果我是一个用户，我会吐槽什么？**
   - 目前只能查天气，如果能查机票、查汇率就好了 → 后续需要扩展更多工具。
   - 每次都要打字问，如果能语音输入就好了。
   - 如果能记住我的常用城市，就不用每次都输入城市名了。

## 今日技术要点

### Function Calling 核心流程

```
用户: "北京天气怎么样？"
    ↓
AI 收到请求（同时被告知有"查天气工具"可用）
    ↓
AI 决策: "这个问题需要调用 get_real_weather 工具"
    ↓
AI 提取参数: {"city_name": "北京"}
    ↓
程序执行: get_real_weather("北京") → 返回 "☀️晴天，26°C"
    ↓
AI 收到工具结果 → 组织语言 → 回复用户
    ↓
用户看到: "北京天气：☀️晴天，温度26°C，风速15km/h"
```

### 关键代码结构

```python
# 1. 定义工具描述（告诉 AI 有这个工具）
tools = [{
    "type": "function",
    "function": {
        "name": "get_real_weather",
        "description": "查询指定城市的实时天气情况",
        "parameters": {...}
    }
}]

# 2. 第一次请求：AI 决策是否调用工具
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=messages,
    tools=tools,
    tool_choice="auto"
)

# 3. 检测到工具调用 → 执行真实函数 → 返回结果
if response_message.tool_calls:
    weather_result = get_real_weather(city)
    
# 4. 第二次请求：AI 根据工具结果生成最终回复
second_response = client.chat.completions.create(
    model="deepseek-chat",
    messages=messages + [tool_result]
)
```


*整理完记得提交到 GitHub：*

```bash
git add .
git commit -m "docs: 更新Day?开发日志，记录Function Calling实现"
git push
```